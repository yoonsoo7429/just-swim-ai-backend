// DB
const dbHost = 'POSTGRES_HOST';
const dbDatabase = 'POSTGRES_DB';
const dbPort = 'POSTGRES_PORT';
const dbUsername = 'POSTGRES_USER';
const dbPassword = 'POSTGRES_PASSWORD';

// JWT
const jwtSecret = 'JWT_SECRET';

// MongoDB
const mongodbUri = 'MONGODB_URI';

export const envVariables = {
  dbHost,
  dbDatabase,
  dbPort,
  dbUsername,
  dbPassword,
  jwtSecret,
  mongodbUri,
};
